pipeline {
    agent any

    stages {
        stage('Greeting') {
            steps {
                echo 'hello, devops team'
            }
        }

        stage('Test Info') {
            steps {
                echo 'my first pipeline testing'
            }
        }

        stage('Wrap Up') {
            steps {
                echo 'tasks completed'
            }
        }
    }
}
